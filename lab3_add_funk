str_get = list(input("Gimmi - "))

def len_len(str):
  len = 0 
  while True:
    try:
      str[len]
      len += 1
    except IndexError:
      break
  return len

x = len_len(str_get)

print(x)